# Shared TypeScript packages

Reusable protocol codecs, the transport-neutral device client, and the
deterministic mock device live here. Applications consume these packages rather
than importing BLE-specific objects into screen code.
